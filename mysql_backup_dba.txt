  Types of Backups in MySQL

   1. Logical Backup
   2. Physical (Raw) Backup 
   3. Snapshot Backup 
   4. Binary log Backup
   5. Incremental / Differential Backups


1. Logical Backup : This is  a dump from logical database structure (CREATE DATABASE  , CREATE TABLE statements) and
content (INSERT statement)


recomended is to be used smaller amount of data 

disadvantage is this method is slower (backup and restore) 
advantage is useful to copy some data to a different environment to run tests 

2. Physical (RAW) Backup : 

In short , this consists of exact copies of database directories and files

copy for all or a part from mysql datadir directory . This kind of backup is most used to restore or create  a new replica node easily and quickly and is used to address the host failure.
 
 it's recomended to restore using the same mysql version .

we can use percona xtrabackup because it can include any related files such as configuration file like cnf config files.



3. Snapshot Backup 

 This provide logical copies of the file system at a given point in time , without requiring a physical copy of the entire file system .

 Mysql itself does not provide the capability of taking file system snapshots but it is available using third party solution like LVM or  ZFS

This disadvantages is that sometimes physical backup do not compress much , because data is usually in a binary format and sometimes the table is already compressed .

4. Binary Log Backups 

 Binlog backup specifically address RPO .

 Binary log file contain records of each SQL query executed that made changes 

 From Mysql 5.6 on , you can use mysqlbinlog to stream binary logs from a remote server

 You can combine binlog backups with Percona Xtrabackup or mydumper backup to allow restoration up to the end of the most-recently-backed-up binary log.

 5. Incremental / Differential Backups

 An incremental backup is a backup of everything that has changed since the last backup (a binary log backup is a special case of an incremental backup)

this is good if your db size is huge as you can take a full backup at the begining of the week and run incremental backup per day.

Incremental backup typically negatively affect the RTO 

Percona Xtrabackup supports both incremental and differential backups .


Encryption 

Backup have sensitive data ,so it's highly recomended to encrypt the especially for offsite storage 

Don't forget to get a copy of the keys/passphrase 


Restore Testing 

 Depending on your business , it's highly recomended to test your backups at least once per month

 Retention Requirements 

30 Days of binlog backup locally on the backup server 

For backup keep in mind you will need a minimum of 2.5 times the current dataset size as free disk space to save/meet 






-------------------------------------------------------------------Binary Log -----------------------------------------------------------------

What is Binary Log 

 set of log file that contain information about data modification made to a mysql server instance

 The log is enabled by starting the server with the --log-bin option

 it contains all statements that update data .
 
 it also contains statements that potentially could have updated it (ex .  a delete which matched no rows) unless row-based logging is used.

-------------------------------------------------------------------Full Database Backup with all data -----------------------------------------------------------------
mysqldump -u root -p --all-databases --routines --triggers --events > all_databases_full.sql

Explanation of options:

--all-databases → dumps every database on the server.

--routines → includes stored procedures and functions.

--triggers → includes triggers.

--events → includes event scheduler events.

(No --no-data) → means it will also include all rows (data).

================-----------OR----------==============

Option 1: Restore only one_web_dialer database from full dump file
sed -n '/^-- Current Database: `one_web_dialer`/,/^-- Current Database: `/p' full_backup.sql > one_web_dialer_backup.sql

Example : below command is fibercate the specific database backup from all database backup file .
[root@localhost MYSQL_Backup]# sed -n '/^-- Current Database: `ONE_WEB_DIALER`/,/^-- Current Database: `/p' /MYSQL_Backup/all_databases_2025-09-23_22-00-01.sql > one_web_dialer_backup.sql

Step 2: Restore the extracted file to MySQL
mysql -u username -p one_web_dialer < one_web_dialer_backup.sql

================-----------OR----------==============

Option 2: Restore full dump but only database one_web_dialer
mysql -u username -p --one-database one_web_dialer < all_databases_2025-09-23_22-00-01.sql


-------------------------------only one dataase----------------------------
mysqldump -u root -p mydatabase --routines --triggers --events > mydatabase_full.sql

-------------------------------Multiple database-----------------------------------
mysqldump -u root -p 1hub_bothub          --routines --triggers --events > /home/sql_backup/1hub_bothub.sql
mysqldump -u root -p CRM_Configuration    --routines --triggers --events > /home/sql_backup/CRM_Configuration.sql
mysqldump -u root -p hr_call_analysis     --routines --triggers --events > /home/sql_backup/hr_call_analysis.sql
mysqldump -u root -p new_1hub_bothub      --routines --triggers --events > /home/sql_backup/new_1hub_bothub.sql
mysqldump -u root -p test_hr_call_analysis --routines --triggers --events > /home/sql_backup/test_hr_call_analysis.sql

--------------------------------------------------------------------------- Backup with different flavour's or only schema-----------------------------------------------------------

1 ) Backup the database without data only schema structure 
 
	mysqldump -u root -p --no-data exampledb > exampledb_schema.sql                		# single database 
	mysqldump -u root -p --no-data --databases db1 db2 db3 > multiple_databases_schema.sql          # multiple database 
	mysqldump -u root -p --no-data --all-databases > all_databases_schema.sql                                  # all database 

	👉 For a true full backup (tables + data + procedures + functions + triggers + events), use:
	mysqldump -u root -p --routines --events databasename > fullbackup.sql
	mysqldump -u root -p --routines --triggers --events databasename > fullbackup.sql

----------------------------------------------------------------------Step-by-Step Guide to Backup Only the Schema of a Single Table----------------------------------------------------------

	mysqldump -u [username] -p --no-data [database_name] [table_name] > [backup_file].sql
	mysqldump -u root -p --no-data exampledb employees > employees_schema.sql


----------------------------------------------------------------------Step-by-Step Guide to Backup store procedure in a database ----------------------------------------------------------
	mysqldump -u username -p --routines --no-data your_database_name > backup.sql
	mysql -u username -p your_database_name < backup.sql # put your db where u want to restore that 

---------------------------------------------------------------------- Backup with different flavour's  only data---------------------------------------------------------------

Step-by-Step Guide to Back Up Only Data  

	mysqldump -u [username] -p --no-create-info [database_name] > [backup_file].sql

	mysqldump -u root -p --no-create-info exampledb > data_backup.sql                               # only data backuped
	mysqldump -u root -p --no-create-info exampledb table1 table2 > data_backup.sql        # backup the table level only data
  	mysql -u [username] -p [database_name] < [backup_file].sql                                         # restore the backup data


----------------------------------------------------------------------------Restoring the single database in all database backup file--------------------------------------------------
✅ Option 1: Use mysql with --one-database (RECOMMENDED)

mysql -u root -p --one-database your_target_database < all_databases_backup.sql
💡 How it works:

--one-database your_target_database tells MySQL to:

Ignore all USE dbname; statements that don’t match.

Ignore all statements targeting other databases.

This assumes that your_target_database already exists.

⚠️ Important Notes

The --one-database option does not skip DDL (like CREATE DATABASE) in the dump file; it just ignores execution of statements not matching your database.

✅ Option 2: Manually Extract Specific Database SQL from the File
	less all_databases_backup.sql
This is more work, but gives you more control.

✅ Option 2: Manually Extract Specific Database SQL from the File

This is more work, but gives you more control.

Find the section for your desired database:
-- Current Database: `your_target_database`

example : mysql -u root -p --one-database db1 < all_databases_backup.sql


----------------------------------------------------------------------------Restoring the database with different flavour's-------------------------------------------------------------

												Restoring Only the Schema
Running: mysql.exe --defaults-file="C:\Users\TEMPON~1.002\AppData\Local\Temp\tmp1ss5ddm4.cnf"  --protocol=tcp --host=20.20.20.82 --user=dba --port=3306 --default-character-set=utf8 --comments --database=CRM_Configuration  < "G:\\20.20.20.82_db_backup\\CRM_Configuration_20250729.sql"

Create the Database:   If the database doesn't already exist, you'll need to create it before restoring the schema.


	mysql -u [username] -p -e "CREATE DATABASE [database_name];"

	Replace [username] with your MySQL username and [database_name] with the name of the database.

Restore the Schema: 

	mysql -u [username] -p [database_name] < [schema_backup].sql


------------------------------------------------------------Restore the Schema of single table in the database ---------------------------------------------------

	mysql -u [username] -p [database_name] < [backup_file].sql
	mysql -u root -p exampledb < employees_schema.sql

    
--------------------------------------------------Restoring Only the Data----------------------------------------------------------
To restore only the data, you need a backup file that includes the data (i.e., INSERT statements) without any schema definitions.

Prepare the Data SQL File:  Ensure you have a .sql file that contains only the data (i.e., INSERT statements without CREATE TABLE statements).


Restore the Data: 
	mysql -u [username] -p [database_name] < [data_backup].sql

	mysql -u root -p exampledb < exampledb_data.sql



Note : 

Error Handling: If there are any constraints or dependencies between tables (such as foreign keys), make sure the order of data restoration respects these dependencies, or disable foreign key checks during the data import with:

SET foreign_key_checks = 0;


SET foreign_key_checks = 1;


-------------------------------------------------------------Step-by-Step Guide to Backup Specific Tables----------------------------------------------



	mysqldump -u [username] -p [database_name] [table1] [table2] ... > [backup_file].sql
	
	[username]: Your MySQL username.
	[database_name]: The name of the database containing the tables you want to back up.
	[table1] [table2] ...: The names of the tables you want to back up.
	[backup_file]: The desired name of your backup file.


	mysqldump -u root -p exampledb employees departments > tables_backup.sql

Restoring Specific Tables from a Backup
	
Ensure the Target Database Exists:

	If the database where you want to restore the tables does not exist, create it first:
	
	mysql -u root -p -e "CREATE DATABASE exampledb;"

Restore the Tables:

	mysql -u root -p exampledb < tables_backup.sql



--- Table backup with specific range								--dbname      --tblname	
mysqldump -u root -p --no-create-info --where="calldate BETWEEN '2024-11-15' AND '2024-11-25'" voiceanalytics calltrans > calltrans_backup.sql


----- Restore table data
mysql -u root -p  new_ui_voiceanalytics(db_name where we want to restore)   < calltrans_backup.sql



---------Take backup except system db
Option 1: Run directly in terminal 
for db in $(mysql -u root -pYOURPASS -N -e "SHOW DATABASES WHERE \`Database\` NOT IN ('mysql','information_schema','performance_schema','sys');"); do
  mysqldump -u root -pYOURPASS --routines --triggers --events --single-transaction $db > /backup/$db.sql
done

Replace YOURPASS with your MySQL password.

Make sure /backup/ exists and is writable.

Option 2: Save as a bash script
create a file 
nano /home/user/mysql_backup.sh

Paste the script:
#!/bin/bash

MYSQL_USER="root"
MYSQL_PASS="YOURPASS"
BACKUP_DIR="/backup"

mkdir -p $BACKUP_DIR

for db in $(mysql -u $MYSQL_USER -p$MYSQL_PASS -N -e "SHOW DATABASES WHERE \`Database\` NOT IN ('mysql','information_schema','performance_schema','sys');"); do
  echo "Backing up $db..."
  mysqldump -u $MYSQL_USER -p$MYSQL_PASS --routines --triggers --events --single-transaction $db > $BACKUP_DIR/$db.sql
done

echo "Backup completed!"


Make it executable:
chmod +x /home/user/mysql_backup.sh

Run any time
/home/user/mysql_backup.sh



✅ Backup all user databases automatically (excluding system DBs)

✅ Step 1: Create the script
sudo nano /usr/local/bin/mysql_backup_each_db.sh

Paste this in .sh
#!/bin/bash

# === CONFIGURATION ===
USER="root"
PASSWORD="your_mysql_password"
BACKUP_DIR="/var/backups/mysql"
DATE=$(date +"%Y-%m-%d")

# Create backup directory
mkdir -p $BACKUP_DIR

# Get list of user databases only
DATABASES=$(mysql -u $USER -p$PASSWORD -e "SHOW DATABASES;" | grep -Ev "(Database|information_schema|performance_schema|mysql|sys)")

# Backup each DB separately
for DB in $DATABASES; do
    mysqldump -u $USER -p$PASSWORD $DB > $BACKUP_DIR/${DB}-${DATE}.sql
done

# Delete backups older than 3 days
find $BACKUP_DIR -type f -name "*.sql" -mtime +3 -exec rm {} \;

Make script executable:
sudo chmod +x /usr/local/bin/mysql_backup_each_db.sh

✅ Step 2: Create cron job
sudo crontab -e

0 2 * * * /usr/local/bin/mysql_backup_each_db.sh




✅ Compressed MySQL Per-Database Backup Script

sudo nano /usr/local/bin/mysql_backup_each_db.sh

#!/bin/bash

# === CONFIGURATION ===
USER="root"
PASSWORD="your_mysql_password"
BACKUP_DIR="/var/backups/mysql"
DATE=$(date +"%Y-%m-%d")

# Create backup directory
mkdir -p $BACKUP_DIR

# Get list of user databases only
DATABASES=$(mysql -u $USER -p$PASSWORD -e "SHOW DATABASES;" | grep -Ev "(Database|information_schema|performance_schema|mysql|sys)")

# Backup each DB separately (compressed)
for DB in $DATABASES; do
    echo "Backing up $DB ..."
    mysqldump -u $USER -p$PASSWORD $DB | gzip > $BACKUP_DIR/${DB}-${DATE}.sql.gz
done

# Delete backups older than 3 days
find $BACKUP_DIR -type f -name "*.sql.gz" -mtime +3 -exec rm {} \;

Make executable:
sudo chmod +x /usr/local/bin/mysql_backup_each_db.sh

✅ Cron Job (Daily at 2 AM)
sudo crontab -e

Add:
0 2 * * * /usr/local/bin/mysql_backup_each_db.sh

✅ Updated script 
sudo nano /usr/local/bin/mysql_backup_each_db.sh


✅ Updated script with log like success and failed log 

#!/bin/bash

# === CONFIGURATION ===
USER="root"
PASSWORD="your_mysql_password"
BACKUP_DIR="/var/backups/mysql"
DATE=$(date +"%Y-%m-%d")

SUCCESS_LOG="/var/log/mysql_backup_success.log"
ERROR_LOG="/var/log/mysql_backup_error.log"

# Create backup directory
mkdir -p $BACKUP_DIR

# Get list of user databases only
DATABASES=$(mysql -u $USER -p$PASSWORD -e "SHOW DATABASES;" | grep -Ev "(Database|information_schema|performance_schema|mysql|sys)")

echo "===== Backup started at $(date) =====" >> $SUCCESS_LOG 2>> $ERROR_LOG

for DB in $DATABASES; do
    OUTPUT_FILE="${BACKUP_DIR}/${DB}-${DATE}.sql.gz"

    # Backup and compress
    if mysqldump --triggers --routines --events --no-tablespaces -u $USER -p$PASSWORD $DB | gzip > "$OUTPUT_FILE" 2>/tmp/mysql_backup_error; then

        echo "$(date): SUCCESS - $DB backup saved to $OUTPUT_FILE" >> $SUCCESS_LOG
    else
        echo "$(date): FAILED - $DB backup" >> $ERROR_LOG
        cat /tmp/mysql_backup_error >> $ERROR_LOG
    fi
done

# Clean up temp file
rm -f /tmp/mysql_backup_error

# Delete backups older than 3 days
find $BACKUP_DIR -type f -name "*.sql.gz" -mtime +3 -exec rm {} \;

echo "Backup finished at $(date)" >> $SUCCESS_LOG
echo "----------------------------------------" >> $SUCCESS_LOG



✅ Method 1: Restore directly from .gz (recommended)

No need to extract the file first.
gunzip -c backupfile.sql.gz | mysql -u root -p database_name

Explanation:

gunzip -c → decompresses the file to stdout
Pipe (|) sends it to MySQL
database_name = the database you want to restore into

✅ Method 2: Extract first, then restore

Extract:
gunzip backupfile.sql.gz

This creates:
backupfile.sql

Restore normally:
mysql -u root -p database_name < backupfile.sql

🔥 Important Notes
✔ You must create the database first (if not exist):

mysql -u root -p -e "CREATE DATABASE database_name;"

✔ If the backup contains DROP/CREATE statements, it will replace the existing DB.

If you want to prevent overwriting, open the .sql file and remove those lines.








