✔ Credentials encrypted (mysql_config_editor)
✔ Backup encrypted (AES-256)
✔ No passwords in scripts
✔ Read-only replica safe
✔ GTID safe
✔ S3 off-site DR



✅ Step 1: Install AWS CLI
cd /tmp
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o awscliv2.zip

--- apt install awscli -y (optional) below is a correct way  

unzip awscliv2.zip
sudo ./aws/install

verify the version
aws --version


✅ Step 2: Configure AWS credentials (secure)
aws configure

Enter:
AWS Access Key ID
AWS Secret Access Key
Region (e.g. ap-south-1)
Output format: json/table/yaml

Credentials stored securely in:
~/.aws/credentials

# to check the conigure is ok or not 
aws configure list

aws sts get-caller-identity


✅ Step 3: Push backups older than 7 days to S3

Example:

Local backup path: /MYSQL_BACKUP

S3 bucket: my-mysql-backups

Folder in S3: prod/mysql

aws s3 sync /MYSQL_BACKUP s3://my-mysql-backups/prod/mysql \
  --exclude "*" \
  --include "*.sql.gz"


✅ Step 4: Move only OLD backups (>7 days) to S3

find /MYSQL_BACKUP -type f -name "*.sql.gz" -mtime +7 -print0 | while IFS= read -r -d '' file
do
  aws s3 cp "$file" s3://my-mysql-backups/prod/mysql/
  rm -f "$file"
done

✅ Step 5: Cron job (Daily at 4 AM)

0 4 * * * /usr/local/bin/mysql_s3_push.sh >> /var/log/mysql_s3.log 2>&1


🔐 S3 BEST PRACTICE

Enable bucket versioning

Enable S3 lifecycle policy:

Move to Glacier after 30 days

Delete after 90 days


-----------------OR Create a Script Below to Push in to our AWS S3 Bucket----------------

✅ FIX STEP 1: Check login-path exists

mysql_config_editor print --all

we see something like this 
[backup]
user = backupuser

if not do below steps

✅ FIX STEP 2: Recreate login-path (IMPORTANT)
Run as root (same user running the script):

mysql_config_editor remove --login-path=backup

Then recreate:

mysql_config_editor set \
  --login-path=backup \
  --user=backupuser \
  --password

----------------OR----------------
mysql_config_editor set \
--login-path=backup \
--user=backup \
--host=localhost \
--password


Enter your user password 

✅ FIX STEP 3: Test manually (MANDATORY)

mysql --login-path=backup -e "SELECT USER();"



--------------Encryption---------------

🔑 STEP 1: Store encryption password securely

Create a root-only readable file:
echo 'MyStrongEncryptionKey@123' > /root/.mysql_backup_enc
chmod 600 /root/.mysql_backup_enc

⚠️ Never hardcode encryption keys in scripts

🧾 STEP 2: Combined Backup + Encrypt + S3 Script

Save as :
vi /usr/local/bin/mysql_backup_s3_encrypt.sh

-----------Backup script-----------

#!/bin/bash

set -e

# ===== CONFIG =====
BACKUP_DIR="/MYSQL_BACKUP"
DATE=$(date +%F)
S3_BUCKET="s3://my-mysql-backups/prod/mysql"
ENC_KEY_FILE="/root/.mysql_backup_enc"
RETENTION_DAYS=7

mkdir -p "$BACKUP_DIR"

ENC_KEY=$(cat "$ENC_KEY_FILE")

# ===== GET DATABASE LIST =====
DATABASES=$(mysql --login-path=backup -N -e "
SHOW DATABASES
WHERE \`Database\`
NOT IN ('information_schema','performance_schema','mysql','sys')
")

# ===== BACKUP EACH DATABASE =====
for DB in $DATABASES
do
  echo "Backing up database: $DB"

  mysqldump \
    --login-path=backup \
    --single-transaction \
    --skip-lock-tables \
    --no-tablespaces \
    --routines \
    --events \
    --triggers \
    --set-gtid-purged=OFF \
    "$DB" \
  | gzip \
  | openssl enc -aes-256-cbc -salt -pbkdf2 -pass pass:"$ENC_KEY" \
  > "$BACKUP_DIR/${DB}_${DATE}.sql.gz.enc"
done

# ===== PUSH OLD BACKUPS TO S3 =====
find "$BACKUP_DIR" -type f -name "*.enc" -mtime +$RETENTION_DAYS -print0 | while IFS= read -r -d '' file
do
  echo "Uploading to S3: $file"
  aws s3 cp "$file" "$S3_BUCKET/"
  rm -f "$file"
done

echo "Backup + encryption + S3 upload completed successfully"


🔧 STEP 3: Permissions

chmod +x /usr/local/bin/mysql_backup_s3_encrypt.sh

⏰ STEP 4: Cron Job (Daily 2 AM)

crontab -e 
0 2 * * * /usr/local/bin/mysql_backup_s3_encrypt.sh >> /var/log/mysql_backup.log 2>&1


🔓 STEP 5: How to DECRYPT & RESTORE (VERY IMPORTANT)
Decrypt:

openssl enc -d -aes-256-cbc -pbkdf2 \
  -in mydb_2026-01-02.sql.gz.enc \
  -out mydb.sql.gz \
  -pass pass:MyStrongEncryptionKey@123

gunzip mydb.sql.gz
mysql mydb < mydb.sql


✅ STEP 1: Confirm the file is encrypted

file /MYSQL_BACKUP/*.enc | head


🔍 Try to read it (should FAIL)
zcat mydb_2026-01-02.sql.gz.enc

🔐 STEP 2: Decrypt test (WITHOUT restoring)
openssl enc -d -aes-256-cbc -pbkdf2 \
  -in /MYSQL_BACKUP/recordings_2026-01-03.sql.gz.enc \
  -out /tmp/recordings_test.sql.gz \   # here in place of tmp u put your own path or put same path where you encypted bakcup genrate 
  -pass file:/root/.mysql_backup_enc

🔍 Verify decrypted file type
file /tmp/recordings_test.sql.gz

📦 STEP 3: Validate SQL content (without restoring)

gunzip -c /tmp/recordings_test.sql.gz | head -20

🧪 STEP 4: REAL VALIDATION (Restore test – safest)
Create test DB

mysql --login-path=backup -e "CREATE DATABASE recordings_restore_test;"

Restore backup
gunzip -c /tmp/recordings_test.sql.gz | mysql recordings_restore_test






